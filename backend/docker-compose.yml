services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      PORT: "3000"
    networks:
      - ai-workflow-net

networks:
  ai-workflow-net:
    driver: bridge
